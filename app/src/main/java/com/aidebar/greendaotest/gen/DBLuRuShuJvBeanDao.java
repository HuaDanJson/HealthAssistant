package com.aidebar.greendaotest.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.demo.bs.demoapp2.DBBean.DBLuRuShuJvBean;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DBLU_RU_SHU_JV_BEAN".
*/
public class DBLuRuShuJvBeanDao extends AbstractDao<DBLuRuShuJvBean, Long> {

    public static final String TABLENAME = "DBLU_RU_SHU_JV_BEAN";

    /**
     * Properties of entity DBLuRuShuJvBean.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property CreatTimeAsId = new Property(0, long.class, "creatTimeAsId", true, "_id");
        public final static Property DataKind = new Property(1, String.class, "dataKind", false, "DBLuRuShuJvBean");
        public final static Property RusultData = new Property(2, String.class, "rusultData", false, "RUSULT_DATA");
        public final static Property HealState = new Property(3, String.class, "HealState", false, "HEAL_STATE");
    }


    public DBLuRuShuJvBeanDao(DaoConfig config) {
        super(config);
    }
    
    public DBLuRuShuJvBeanDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DBLU_RU_SHU_JV_BEAN\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: creatTimeAsId
                "\"DBLuRuShuJvBean\" TEXT," + // 1: dataKind
                "\"RUSULT_DATA\" TEXT," + // 2: rusultData
                "\"HEAL_STATE\" TEXT);"); // 3: HealState
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DBLU_RU_SHU_JV_BEAN\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DBLuRuShuJvBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCreatTimeAsId());
 
        String dataKind = entity.getDataKind();
        if (dataKind != null) {
            stmt.bindString(2, dataKind);
        }
 
        String rusultData = entity.getRusultData();
        if (rusultData != null) {
            stmt.bindString(3, rusultData);
        }
 
        String HealState = entity.getHealState();
        if (HealState != null) {
            stmt.bindString(4, HealState);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DBLuRuShuJvBean entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.getCreatTimeAsId());
 
        String dataKind = entity.getDataKind();
        if (dataKind != null) {
            stmt.bindString(2, dataKind);
        }
 
        String rusultData = entity.getRusultData();
        if (rusultData != null) {
            stmt.bindString(3, rusultData);
        }
 
        String HealState = entity.getHealState();
        if (HealState != null) {
            stmt.bindString(4, HealState);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public DBLuRuShuJvBean readEntity(Cursor cursor, int offset) {
        DBLuRuShuJvBean entity = new DBLuRuShuJvBean( //
            cursor.getLong(offset + 0), // creatTimeAsId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // dataKind
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // rusultData
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // HealState
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DBLuRuShuJvBean entity, int offset) {
        entity.setCreatTimeAsId(cursor.getLong(offset + 0));
        entity.setDataKind(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setRusultData(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setHealState(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DBLuRuShuJvBean entity, long rowId) {
        entity.setCreatTimeAsId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DBLuRuShuJvBean entity) {
        if(entity != null) {
            return entity.getCreatTimeAsId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DBLuRuShuJvBean entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
